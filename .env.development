# Development Environment Variables
# This file is used for local development

# API Base URL for development
# Option 1: Use localhost (only accessible from same machine)
# VITE_API_BASE_URL=http://localhost:3001/api

# Option 2: Use your local network IP (accessible from other devices on same network)
# Replace <your-ip> with your actual IP address (e.g., 192.168.0.28)
# VITE_API_BASE_URL=http://192.168.0.28:3001/api

# Option 3: Use 0.0.0.0 (will resolve to localhost in browser)
VITE_API_BASE_URL=http://localhost:3001/api

# For production deployment, use .env.production instead
