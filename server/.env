# Backend Server Environment Variables

# Database Configuration
DB_HOST=103.14.123.44
DB_PORT=30018
DB_NAME=wb_lead
DB_USER=dhi_admin
DB_PASSWORD=dhi@123

# Server Configuration
PORT=3001
NODE_ENV=development

# Network Configuration
# Server will bind to 0.0.0.0 to be accessible from any network interface
# This allows access from:
# - localhost (127.0.0.1)
# - Local network IP (e.g., 192.168.x.x)
# - Public IP (if deployed on server)
